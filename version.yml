# Change this file to trigger a full rebuild.
# Usually not necessary. Changes to images.yml and *.Dockerfile will be detected via the Makefile cache.
# If the Dockerfile uses another file and _that_ file changes, that won't be detected. Change the number in that case.
version: 1